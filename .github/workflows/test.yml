name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  shell-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check shell syntax
        run: bash -n bin/ccx scripts/install.sh scripts/doctor.sh tests/stub-claude.sh tests/test-ccx.sh
      - name: Test launcher behavior
        run: bash tests/test-ccx.sh
